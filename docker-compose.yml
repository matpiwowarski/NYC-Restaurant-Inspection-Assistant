version: "3.8"

services:
  mongo:
    image: mongo:6.0
    container_name: nyc_inspector_mongo
    ports:
      - "27017:27017"
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
